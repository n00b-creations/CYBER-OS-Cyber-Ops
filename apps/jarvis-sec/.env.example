# Development-only API bridge. Never place production credentials in VITE_* variables.
VITE_API_BASE_URL=http://localhost:8787
VITE_DEV_ACCESS_TOKEN=

# Production authentication is intentionally server/session mediated and should
# not expose long-lived tokens to the Vite bundle.
